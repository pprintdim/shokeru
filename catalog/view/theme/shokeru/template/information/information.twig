{{ header }}
{{ content_top }}
	<section class="infoPage infoPage-{{ slug }}">
        <div class="container">
            <div class="infoPage__inner">
                <h1 class="infoPage__title main-title">{{ heading_title }}</h1>
                 <p class="infoPage__name">{{ meta_description }}</p>
                  {{ description }}
            </div>
        </div>
    </section>
{{ content_bottom }}
{{ footer }}